[build]
  command = "npm run build"
  publish = "dist"

# Skip secret scanning for Firebase/Cloudinary client-side config
# These are NOT secrets - they are public client-side configuration
[build.environment]
  SECRETS_SCAN_ENABLED = "false"
